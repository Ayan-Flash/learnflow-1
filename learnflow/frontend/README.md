# LearnFlow AI — Frontend

Next.js + TypeScript + Tailwind student UI for LearnFlow AI.

## Setup

```bash
cd frontend
npm install
npm run dev
```

## Environment

Set the backend URL via:

- `NEXT_PUBLIC_BACKEND_URL` (e.g. `http://localhost:8080`)

If not set, the UI defaults to `http://localhost:8080`.
